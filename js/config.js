require.config({
  // init app, require will look for an app.js in the same folder as this config
  deps : ['app'],

  // define paths
  paths : {
    vendor : 'vendor',
    jquery : 'vendor/jquery'
  }
});
